<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryDark">@color/primary_dark_green</item>
        <item name="colorAccent">@color/accent_mint</item>
        <item name="colorControlActivated">@color/accent_dark_mint</item>
        <item name="colorControlHighlight">@color/accent_light_mint</item>
        <item name="colorControlNormal">@color/accent_mint</item>

        <item name="android:actionBarStyle">@style/ActionBarTheme</item>
        <item name="android:buttonStyle">@style/Button</item>
    </style>

    <style name="ActionBarTheme" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/primary_green</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Button" parent="android:Widget.DeviceDefault.Button.Borderless">
        <item name="android:textColor">@color/button_textcolor_selector</item>
        <item name="android:padding">8dp</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:height">36dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@drawable/button_selector_background</item>
    </style>

    <style name="FlatButton">
        <item name="android:textColor">@color/flatbutton_textcolor_selector</item>
        <item name="android:padding">8dp</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:height">36dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@drawable/flatbutton_selector_background</item>
    </style>

    <style name="AlertDialogTheme" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/accent_mint</item>
    </style>

    <style name="AppThemeTextViewSubtitle">
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">@dimen/textView_normal_textViews</item>
    </style>

    <style name="AppTheme.normalText">
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textSize">@dimen/textView_normal_textViews</item>
    </style>
</resources>
